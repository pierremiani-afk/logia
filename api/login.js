import {config,send} from './_supabase.js';
export default async function handler(req,res){if(req.method!=='POST')return send(res,405,{error:'Méthode non autorisée.'});try{const{url,key}=config();const response=await fetch(`${url}/auth/v1/token?grant_type=password`,{method:'POST',headers:{apikey:key,'Content-Type':'application/json'},body:JSON.stringify(req.body)});const data=await response.json();if(!response.ok)return send(res,response.status,{error:data.error_description||data.msg||'Connexion impossible.'});return send(res,200,{access_token:data.access_token,email:data.user?.email||req.body.email});}catch(error){return send(res,500,{error:error.message});}}
